package rules.triage.level1

import com.ftn.model.AgeCategory;
import com.ftn.model.Patient;

rule "Classify senior patient"
    salience 1000
when
    $patient : Patient(ageCategory == null, age > 65)
then
    modify($patient) {
        setAgeCategory(AgeCategory.SENIOR);
    }
end
