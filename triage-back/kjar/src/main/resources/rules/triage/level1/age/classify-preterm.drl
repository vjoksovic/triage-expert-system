package rules.triage.level1.age

import com.ftn.model.AgeGroup;
import com.ftn.model.Patient;

rule "Classify preterm patient"
    salience 1000
when
    $patient : Patient(ageGroup == null, preterm == true)
then
    modify($patient) {
        setAgeGroup(AgeGroup.PRETERM);
    }
end
